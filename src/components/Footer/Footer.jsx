import React from 'react';
import { StyledFooter } from 'components/Footer/Footer.styled';
import Typography from '@mui/material/Typography';

const Footer = () => {
  return (
    <StyledFooter>
      <Typography variant="subtitle2">(c) Chackmool </Typography>
    </StyledFooter>
  );
};

export default Footer;
